name: Publish Release

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: windows-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
      
      - name: Install dependencies
        run: pnpm install
      
      - name: Build and publish
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: pnpm dist:publish

      - name: Send WeChat Notification
        if: success()
        run: |
          $version = "${{ github.ref_name }}"
          $repo = "${{ github.repository }}"
          $runId = "${{ github.run_id }}"
          $downloadUrl = "https://github.com/$repo/releases/tag/$version"
          $buildUrl = "${{ github.server_url }}/$repo/actions/runs/$runId"
          
          $content = @"
          <h2>🎉 发布成功</h2>
          <p><strong>版本号：</strong>$version</p>
          <p><strong>状态：</strong>✅ 构建和发布成功</p>
          <p><strong>下载链接：</strong><a href="$downloadUrl">点击下载</a></p>
          <p><strong>构建详情：</strong><a href="$buildUrl">查看构建日志</a></p>
          "@
          
          $body = @{
            token = "${{ secrets.PUSHPLUS_TOKEN }}"
            title = "🎉 摸了吗 - 新版本发布成功"
            content = $content
            template = "html"
          } | ConvertTo-Json
          
          Invoke-RestMethod -Uri "http://www.pushplus.plus/send" -Method Post -ContentType "application/json" -Body $body